import 'package:flutter/material.dart';

class WidgetsVsJsx extends StatelessWidget {
  const WidgetsVsJsx({super.key});

  @override
  Widget build(BuildContext context) {
    return Center(child: Text('Pantalla de (Widgets vs JSX RN)'));
  }
}