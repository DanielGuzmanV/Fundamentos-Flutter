import 'package:flutter/material.dart';

class IntermediateConceptsScreen extends StatelessWidget {
  const IntermediateConceptsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pantalla para conceptos intermedios'),
    );
  }
}