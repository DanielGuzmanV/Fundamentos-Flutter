import 'package:flutter/material.dart';

class AdvancedConceptsScreen extends StatelessWidget {
  const AdvancedConceptsScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return const Center(
      child: Text('Pantalla para concepts avanzados'),
    );
  }
}